# Model Registry and Version History

Every model version below is tracked in MLflow with its parameters, metrics and artefacts. Open the tracking UI with:

```bash
mlflow ui --backend-store-uri sqlite:///part3_machine_learning/mlflow.db
```

All metrics are on the **chronological** test split (the final 20% of the timeline, 2017-10-26 to 2018-09-30), which is the deployment-realistic figure.

## Registered models

| Registered name | Version | Alias | Source run | Key metric | Artefact |
| --- | ---: | --- | --- | ---: | --- |
| `traffic_risk_classifier` | 1 | **champion** | `clf_random_forest` | roc_auc = 0.9963 | `models:/m-5f7b1da8364047e0a502690d31a3768f` |
| `traffic_risk_classifier` | 2 | **challenger** | `clf_logistic_regression` | roc_auc = 0.9930 | `models:/m-6e707e59be42476db12a52a276780c97` |
| `traffic_volume_regressor` | 1 | **champion** | `reg_hist_gradient_boosting` | mae = 225.7681 | `models:/m-d0d70ae3116a4617b7171a4b570a1e21` |
| `traffic_volume_regressor` | 2 | **challenger** | `reg_linear_regression` | mae = 645.0337 | `models:/m-52144d8cd9424cdd9e09c2bc2e606849` |
| `traffic_lstm_forecaster` | 1 | **champion** | `lstm_traffic_forecast` | mae = 255.7428 | `models:/m-6349f963571349118e72c209cfb5cc4a` |

## Version-by-version performance

### Classification — proxy accident risk

| Run | Accuracy | Precision | Recall | F1 | ROC AUC | AUC on random split | Train (s) |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `clf_logistic_regression` | 0.9632 | 0.8180 | 0.9892 | 0.8955 | **0.9930** | 0.9960 | 0.7 |
| `clf_random_forest` | 0.9829 | 0.9164 | 0.9822 | 0.9482 | **0.9963** | 0.9990 | 2.0 |

### Regression and forecasting — hourly traffic volume

| Run | MAE (vehicles) | MAE % of mean | RMSE | R² | Train (s) |
| --- | ---: | ---: | ---: | ---: | ---: |
| `reg_linear_regression` | **645.0** | 19.5% | 861.6 | 0.8087 | 0.1 |
| `reg_hist_gradient_boosting` | **225.8** | 6.8% | 372.5 | 0.9643 | 2.7 |
| `lstm_traffic_forecast` | **255.7** | 7.7% | 320.0 | 0.9740 | 186.3 |

## Promotion policy

A version is promoted to **champion** when it beats the incumbent on the ranking metric on the chronological split *and* the monitoring dashboard shows no open error-drift alert for the incumbent that the challenger would inherit. Promotion is a human decision recorded as an MLflow alias change; it is never automatic.

## Reproducibility

Every run records its hyperparameters (`hp_*`), the feature count, the train/test row counts and the split method as MLflow parameters, and the trained estimator as an artefact. Re-running `supervised.py` and `deep_learning.py` creates new runs alongside the old ones rather than overwriting them, so the history is preserved.

_Generated by `part3_machine_learning/src/model_registry.py`._